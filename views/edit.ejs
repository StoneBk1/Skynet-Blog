<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Post</title>
    <link rel="stylesheet" href="/form.css">
</head>
<body>
    <div class="post-form">
        <%- include('partials/header.ejs') %> 
        <h2>edit The  Post</h2>
        <form action="/edit/<%= postData.id %>?_method=PATCH" method="post">

            <div class="form-group">
                <label for="title">Title</label>
                <input type="text" class="title" name="title" value="<%= postData.title %>" required>
            </div>
            <div class="form-group">
                <label for="description">Description</label>
                <textarea id="description" name="description" rows="5" required><%= postData.description %></textarea>
            </div>
            <div class="form-group">
                <label for="content">Content</label>
                <textarea id="content" name="content" rows="10" required><%= postData.content %></textarea>
            </div>
            <button type="submit">Save the post</button>
        </form>
    </div>
</body>
</html>